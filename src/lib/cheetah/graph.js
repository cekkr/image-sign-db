// Cheetah's graph and associative-recall commands — owned by the submodule's
// Node binder (cheetah/binders/nodejs/lib/graph.js).
//
// Used by the sign pipeline; the delta family does not touch the graph.

module.exports = require('./binder').graph;
